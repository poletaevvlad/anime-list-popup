{
  "name": "animelist-ext",
  "version": "0.0.1",
  "scripts": {
    "build": "npx gulp release",
    "watch": "npx gulp watch",
    "package": "rm -f dist.zip source.zip && cd dist && zip -r ../dist.zip * && cd .. && zip -r source.zip assets html sass src screenshots package.json package-lock.json test-preprocessor.js tsconfig.json LICENSE gulpfile.js"
  },
  "devDependencies": {
    "@types/react": "^16.9.49",
    "@types/react-dom": "^16.9.8",
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-sass": "^4.1.0",
    "ts-loader": "^8.0.4",
    "typescript": "^4.0.3",
    "webextension-polyfill-ts": "^0.20.0",
    "webpack-stream": "^6.1.2"
  },
  "dependencies": {
    "async-mutex": "^0.2.4",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
